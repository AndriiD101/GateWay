services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: gateway-app:latest
    container_name: gateway-backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
